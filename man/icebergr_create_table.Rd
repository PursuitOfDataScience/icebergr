% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{icebergr_create_table}
\alias{icebergr_create_table}
\title{Create an Iceberg table}
\usage{
icebergr_create_table(catalog, table, data, location = NULL)
}
\arguments{
\item{catalog}{An \code{icebergr_catalog} from \code{\link[=icebergr_catalog]{icebergr_catalog()}}.}

\item{table}{A table identifier, \code{"namespace.table"}. The namespace must
already exist; see \code{\link[=icebergr_create_namespace]{icebergr_create_namespace()}}.}

\item{data}{A data frame whose columns define the schema. No rows are written;
only the column names and types are used. An Arrow schema is also accepted.}

\item{location}{Where to store the table. \code{NULL} lets the catalog decide,
which is almost always what you want.}
}
\value{
An \code{icebergr_table} handle for the new, empty table.
}
\description{
The table's schema is taken from \code{data}, so an existing data frame is enough
to define one. Iceberg field ids are assigned automatically, since a data
frame has no concept of them.
}
\details{
The table is created unpartitioned. Partitioned table creation, like partition
evolution, is out of scope for this version; see \code{\link[=icebergr_spec_support]{icebergr_spec_support()}}.
}
\examples{
warehouse <- tempfile("warehouse")
dir.create(warehouse)
catalog <- icebergr_catalog("memory", warehouse = warehouse)
icebergr_create_namespace(catalog, "db")

tbl <- icebergr_create_table(
  catalog, "db.events",
  data.frame(id = integer(), amount = double(), label = character())
)
icebergr_schema(tbl)
}

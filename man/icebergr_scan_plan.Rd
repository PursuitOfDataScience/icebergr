% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan.R
\name{icebergr_scan_plan}
\alias{icebergr_scan_plan}
\title{Inspect the file plan for a scan}
\usage{
icebergr_scan_plan(scan)
}
\arguments{
\item{scan}{An \code{icebergr_scan} from \code{\link[=icebergr_scan]{icebergr_scan()}}.}
}
\value{
A tibble with one row per planned file task: \code{data_file_path},
\code{record_count}, \code{file_size_in_bytes}, \code{start} and \code{length}.

\code{record_count} is \code{NA} for a task covering part of a file, since a partial
read has no meaningful record count from the manifest.
}
\description{
Reports which data files a scan would read, without reading them. This is how
pushdown is verified rather than assumed: a filtered scan should plan fewer
files, and fewer records, than an unfiltered one.
}
\examples{
\dontrun{
all_files <- icebergr_scan_plan(icebergr_scan(tbl))
hot_files <- icebergr_scan_plan(icebergr_scan(tbl, filter = year == 2024))
sum(hot_files$record_count) < sum(all_files$record_count)
}
}

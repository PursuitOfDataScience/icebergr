% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{icebergr_catalog}
\alias{icebergr_catalog}
\title{Connect to an Iceberg catalog}
\usage{
icebergr_catalog(
  type = c("rest", "memory", "glue"),
  uri = NULL,
  warehouse = NULL,
  ...,
  storage = c("auto", "local", "s3"),
  name = "icebergr"
)
}
\arguments{
\item{type}{Catalog type. \code{"rest"} for an Iceberg REST catalog, \code{"memory"} for
an in-process catalog over a local warehouse directory, \code{"glue"} for AWS
Glue.

There is deliberately no \code{"hadoop"} option: \code{iceberg-rust} does not
implement a Hadoop or filesystem catalog. Use \code{type = "memory"} with
\code{warehouse} for a table on local disk.}

\item{uri}{Catalog URI. Required for \code{type = "rest"}, ignored otherwise.}

\item{warehouse}{Warehouse location. A directory for \code{type = "memory"}; for
REST catalogs, the warehouse name or location the server expects.}

\item{...}{Further catalog properties, passed through to \code{iceberg-rust} as
name-value pairs. Use this for non-secret configuration such as
\code{"s3.endpoint"} or \code{"rest.signing-region"}.}

\item{storage}{Storage backend. \code{"auto"} infers it from \code{warehouse},
\code{"local"} forces the local filesystem, \code{"s3"} forces object storage. S3
requires the package to have been compiled with the \code{s3} Cargo feature.}

\item{name}{A label for the connection, used in error messages.}
}
\value{
An \code{icebergr_catalog} object.
}
\description{
Connect to an Iceberg catalog
}
\section{Credentials}{

Credentials are read from environment variables, never from arguments:

\describe{
\item{\code{ICEBERGR_REST_TOKEN}}{Bearer token for a REST catalog.}
\item{\code{ICEBERGR_REST_CREDENTIAL}}{OAuth2 client credential.}
\item{\code{ICEBERGR_REST_OAUTH2_SERVER_URI}}{OAuth2 token endpoint.}
\item{\code{ICEBERGR_REST_SCOPE}}{OAuth2 scope.}
\item{\code{ICEBERGR_S3_ACCESS_KEY_ID}, \code{ICEBERGR_S3_SECRET_ACCESS_KEY},
\code{ICEBERGR_S3_SESSION_TOKEN}}{Object storage credentials. The standard
\verb{AWS_*} variables are used as a fallback.}
}

Catalog properties are never printed, logged or included in error messages.
A credential property passed through \code{...} anyway is accepted but warned
about, since a script is the one place it should not be.
}

\examples{
# A local warehouse needs no catalog server and no credentials.
warehouse <- tempfile("warehouse")
dir.create(warehouse)
catalog <- icebergr_catalog("memory", warehouse = warehouse)
catalog

\dontrun{
# A REST catalog. The token comes from the environment, not from here.
Sys.setenv(ICEBERGR_REST_TOKEN = "...")
catalog <- icebergr_catalog("rest", uri = "https://catalog.example.com")
}
}

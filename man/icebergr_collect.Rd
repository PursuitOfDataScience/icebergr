% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan.R
\name{icebergr_collect}
\alias{icebergr_collect}
\alias{icebergr_collect.icebergr_scan}
\alias{icebergr_collect.icebergr_table}
\alias{as.data.frame.icebergr_scan}
\alias{as.data.frame.icebergr_table}
\title{Materialise a scan or a table}
\usage{
icebergr_collect(x, ...)

\method{icebergr_collect}{icebergr_scan}(x, ...)

\method{icebergr_collect}{icebergr_table}(x, ...)

\method{as.data.frame}{icebergr_scan}(x, row.names = NULL, optional = FALSE, ...)

\method{as.data.frame}{icebergr_table}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{An \code{icebergr_scan} from \code{\link[=icebergr_scan]{icebergr_scan()}}, or an \code{icebergr_table}
(equivalent to scanning all of it).}

\item{...}{Unused, for S3 consistency.}

\item{row.names}{Unused, for consistency with \code{\link[base:as.data.frame]{base::as.data.frame()}}.}

\item{optional}{Unused, for consistency with \code{\link[base:as.data.frame]{base::as.data.frame()}}.}
}
\value{
A tibble.
}
\description{
Materialise a scan or a table
}
\details{
Data crosses from Rust into R over the Arrow C stream interface, so batches
are handed over by pointer rather than serialised.

If the dplyr package is installed, \code{dplyr::collect()} also works on these
objects.
}
\examples{
tbl <- icebergr_example_table(rows = 10)

# A scan, materialised
icebergr_collect(icebergr_scan(tbl, filter = id > 1000, select = c("id", "amount")))

# A whole table, materialised
icebergr_collect(tbl)
}
